const { reloadApp } = require('./utils')

describe('Home Screen Tests', () => {
  beforeEach(async () => {
    await device.launchApp({ url: 'exp://localhost:8081' })
  })

  it('should have welcome screen', async () => {
    await waitFor(element(by.id('homeScreen'))).toBeVisible().withTimeout(200000)
  })
})
